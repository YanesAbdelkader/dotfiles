# Basic wofi config

# Prompt text
prompt=Run:

# Window size
width=600
height=400

# Position (center, top, bottom, left, right)
location=center

# Hide scrollbar
hide-scroll=true

# Show matching mode (normal, fuzzy, prefix)
matching=fuzzy

# Show icons (requires .desktop files with icons)
show=drun
